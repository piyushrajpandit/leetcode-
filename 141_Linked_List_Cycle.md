#approach 
1. use mapping so map every link list with false and name it visited 
2. when you pass them mark them true ; 
3, when when you reach a link list marked true already with then that the loop return false 
map <Node * , bool>visited;
this is the syntax to mapping 
