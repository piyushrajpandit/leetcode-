1. crete dummy heads
2. crete tail   Node * zerotail = zerohead;
3. now crete three seperate list for zero one and two
4. if curr value == 0 then add that to zero link list else other
5. now merge list for merging use if because may one link list can be empty
6. setup head 
7. delete the dummy heads
