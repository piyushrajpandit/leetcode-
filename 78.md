#just use recursion 
you have to do three thing 
1. is increase by iteration i from first element to next
2. you have to exlude the element that you are at
3. you have to include that element at that element you are at
4. actual you have use recursion with base case that index == size then push the answer into vector and return.
