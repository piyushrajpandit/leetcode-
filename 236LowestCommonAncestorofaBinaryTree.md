space o(height)
time o(n)

use recurssion 
1. if root null return null
2. if root val = p or root val return n return root 
now left call 
and right call
if leftans != NULL and right is also !=NULL retrn root 
else if left!= NULL and roght == NULL return right 
simarly for right and return NULL if none of them 
